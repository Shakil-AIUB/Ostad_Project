name: github action ci cd

on:
  push:
    branches:
      - main
pull_request:
    branches:
      - main



jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: read hello.txt
        run: echo "Hello Ostad. Welcome to ostad nama" > hello.txt 
  build_job:
      - name: test_job
        runs-on: ubuntu-latest
        steps:
      - name: Git Clone
        uses: actions/checkout@v2
        
      - name: set Up node.js
        uses: actions/setup-node@v2
        with:
          node-version: '16'

          name: Install dependencies